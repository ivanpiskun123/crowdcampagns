<%= form_for ([@campaign, @campaign.comments.build]) do |f| %>
  <%= f.text_field :content, class:"form-control form-group" ,placeholder: "Add a public comment"  %>
  <%= f.submit "Comment", class: "btn btn-outline-primary form-group btn-sm" %>
<% end %>
